class Constants:
    """Application-wide constants.
    
    Store non-sensitive configuration and magic numbers here.
    Secrets should go in env/local.env or env/docker.env.
    """
    APP_NAME = "SellIt"
    VERSION = "0.1.0"
    
    # Example constants
    DEFAULT_PAGE_SIZE = 20
    MAX_RETRIES = 3
